pipeline {
    agent any
    
    stages {
        stage('DR Testing') {
            steps {
                echo 'Running DR tests'
                sh 'terraform init'
                sh 'terraform plan'
            }
        }
    }
}